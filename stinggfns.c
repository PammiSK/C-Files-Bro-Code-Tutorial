#include <stdio.h>
#include <string.h>

int main() {
	/*
	strlwr(string1); convert to lowercase
	strupr(string1); convert to uppercase
	strcat(string1, string2); concatenate s2 to end of s1 - s1s2
	strncat(string1, string2, n); concatenate n characters from s2 to s1
	strcpy(string1, string2); copy s2 to s1
	strncpy(string1, string2, n); copy n chars from s2 to s1

	strset(string1, '?'); sets all chars of s1 to given char (?)
	strnset(string1, '?', n) sets n chars of s1 to given char (?)
	strrev(string1)  reverses a string

	int result = strlen(string1) returns string length as int
	int result = strcmp(string1) string compare all characters, 0 if same
	int result = strncmp(string1) string compare n characters
	int result = strcmpi(string1) string compare all characters (ignore case sensetivity)
	int result = strnicmp(string1) string compare n characters (ignore case sensetivity)
	*/


	return 0;
	//clrscr();
}